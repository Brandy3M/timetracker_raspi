Beispiel: Aufbau einer Git-Versionsverwaltung für eine Web-App
1. Initialisierung des Projekts
Du hast einen Projektordner my-webapp.

Mit folgendem Befehl wird Git aktiviert:

bash
git init
Ergebnis: Ein versteckter .git-Ordner wird erstellt, der alle Versionsinformationen enthält.

2. Erste Dateien hinzufügen
Du erstellst eine index.html und eine style.css.

Um diese Dateien zur Versionsverwaltung hinzuzufügen:

bash
git add index.html style.css
git commit -m "Initial commit: Grundgerüst der Web-App"
Damit hast du den ersten „Snapshot“ deiner Arbeit gesichert.

3. Zusammenarbeit über ein Remote-Repository
Du legst ein Repository bei GitHub oder GitLab an.

Verknüpfung mit deinem lokalen Projekt:

bash
git remote add origin https://github.com/username/my-webapp.git
git push -u origin main
Nun ist dein Projekt zentral gespeichert und für andere zugänglich.

4. Branches für neue Features
Du willst ein neues Login-Feature entwickeln:

bash
git checkout -b feature/login
In diesem Branch arbeitest du unabhängig vom Hauptzweig (main).

Nach Fertigstellung:

bash
git add login.html login.js
git commit -m "Login-Feature hinzugefügt"
git checkout main
git merge feature/login
Vorteil: Saubere Trennung von Entwicklungssträngen.

5. Versionskontrolle und Historie
Mit git log siehst du die gesamte Commit-Historie.

Jeder Commit ist ein klar dokumentierter Entwicklungsschritt.

Beispielausgabe:

Code
commit 1a2b3c4d
Author: Marvin
Date:   Sat Nov 15 11:00 2025

    Login-Feature hinzugefügt
6. Konfliktmanagement
Wenn zwei Entwickler dieselbe Datei ändern, kann ein Merge-Konflikt entstehen.

Git markiert die Konfliktstellen:

html
<<<<<<< HEAD
<h1>Startseite</h1>
=======
<h1>Homepage</h1>
>>>>>>> feature/homepage
Lösung: Manuell entscheiden, welche Version übernommen wird, dann committen.

7. Versionierung und Releases
Mit Tags kannst du stabile Versionen markieren:

bash
git tag -a v1.0 -m "Erste stabile Version"
git push origin v1.0
So entsteht eine klare Versionshistorie für Releases.

🧩 Zusammenfassung
Aufbau: git init, Dateien hinzufügen, Remote-Repo verknüpfen.

Nutzung: Branches für Features, Commits für Änderungen, Merges für Integration.

Verwaltung: Logs und Tags für Historie und Releases, Konfliktlösung für Teamarbeit.

👉 Für deine didaktische Arbeit kannst du dieses Beispiel leicht in eine Übungsaufgabe verwandeln:

Gruppe A erstellt ein Feature-Branch, Gruppe B ein anderes.

Am Ende werden beide Branches zusammengeführt, Konflikte gelöst und ein Release-Tag gesetzt.

Willst du, dass ich dir daraus ein aktivierendes Gruppenformat baue – z. B. ein Rollenspiel, bei dem die Teilnehmer verschiedene Rollen im Git-Workflow übernehmen (Entwickler, Reviewer, Release-Manager)